<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring_react.spring_react.bookregister.BookImageMapper">

    <insert id="insertBookImage">
        INSERT INTO book_image
            (image_id, book_id, image_url, is_thumbnail, created_at)
        VALUES
            (#{imageId}, #{bookId}, #{imageUrl}, #{isThumbnail}, NOW())
    </insert>

</mapper>
